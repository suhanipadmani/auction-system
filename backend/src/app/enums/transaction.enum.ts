export enum TRANSACTION_TYPES {
    CREDIT = "credit",
    DEBIT = "debit",
    LOCK = "lock",
    UNLOCK = "unlock"
}

export enum TRANSACTION_SOURCES {
    DEPOSIT = "deposit",
    ADMIN = "admin",
    BID = "bid"
}

export enum TRANSACTION_STATUSES {
    SUCCESS = "success",
    FAILED = "failed"
}
